# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Environment
.env

# Databases (runtime data)
*.db
*.db-shm
*.db-wal

# OAuth credentials & tokens
credentials/

# Extracted artefact cache (large, regenerable)
.cache/

# Qdrant vector store data — decommissioned 2026-07 (migrated to LanceDB art_index
# table); kept as a backup pending user cleanup, not regenerable without re-running
# tools/build_art_index.py from scratch
qdrant_storage/
qdrant_storage.migrated-backup/

# LanceDB vector store data (large, regenerable via Rebuild Index)
lancedb_storage/

# Stray shortcuts
*.lnk

# Third-party binaries (Poppler, etc.)
bin/

# Claude Code session data
.claude/

# AI tool state
.gemini/
.superpowers/

# OS
.DS_Store
Thumbs.db
